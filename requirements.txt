# AI Editor Assistant — Python Dependencies
#
# Install with:  py -3.12 -m pip install -r requirements.txt
#
# Python 3.10–3.12 only. DaVinci Resolve's compiled scripting module
# (DaVinciResolveScript.pyd) segfaults on Python 3.13+.
#
# Also required (not pip-installable):
#   ffmpeg — https://ffmpeg.org/download.html
#   Add ffmpeg to your system PATH after installation.

pydub>=0.25.1       # Audio loading and silence detection
requests>=2.31.0    # ElevenLabs API HTTP calls
numpy>=1.26.0       # RMS audio analysis for zoom detection
customtkinter>=5.2.0  # GUI framework (replaces Fusion UIManager, removed in Resolve free v19.1)

Pillow>=10.0.0      # Icon generation (generate_icon.py) and any future image ops

faster-whisper>=1.0.0  # Local transcription via CTranslate2 (no PyTorch needed)

yake>=0.4.8            # Statistical keyword extractor (B-roll search), no model download
spacy>=3.7.0           # NLP keyword extraction for B-roll search (noun chunks + NER)
en-core-web-sm @ https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.8.0/en_core_web_sm-3.8.0-py3-none-any.whl

sentence-transformers>=2.7.0  # Semantic B-roll clip matching via all-MiniLM-L6-v2 (~80MB, no API key)
keybert>=0.8.0                # Semantic keyword extraction for online B-roll search (uses sentence-transformers)
joblib>=1.4.0                 # Embedding cache — avoids re-encoding the same transcript on repeated runs

silero-vad>=5.0        # Neural VAD silence detection — handles noise, music beds, quiet speakers
torchaudio>=2.5.0,<2.9 # Audio I/O backend for silero-vad; pinned <2.9 because 2.9+ requires torchcodec
                        # which needs FFmpeg full-shared DLLs (Windows static build not supported)
soundfile>=0.12.0      # torchaudio <2.9 audio backend on Windows (no DLLs needed)
onnxruntime>=1.18.0    # silero-vad ONNX backend (~20 MB); avoids full PyTorch install

# Optional — speeds up silence detection on large files:
# librosa>=0.10.0
